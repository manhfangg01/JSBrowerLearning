const name = "Manh";
const age = 1999;

const intro = `Hello my name is ${name},
currentl I am ${age} years-old `;
// Chèn trực tiếp biến và biểu thức không cần cộng chuỗi
// Không cần /n để xuống dòng
console.log(intro);
